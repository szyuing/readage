# Reading Edge

无需 AI 的英语阅读定级 MVP。系统根据国内外考试成绩、成绩年份、阅读自评和学习阶段，保守推算 A1–C2 起始档位；随后用同级阅读实测校准结果。

内置 A1–C2 共 12 套固定测试（每级 A/B 两卷），每卷包含文章、点词语境释义，以及词汇、句子、语篇三个层次的 6 道题。

## 本地运行

```bash
npm install
npm run dev
```

打开 `http://localhost:3000`。

运行完整检查：

```bash
npm test
npm run lint
```
