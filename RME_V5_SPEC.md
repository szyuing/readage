# Reading Memory Engine V5（RME-V5）规格

## 定位

V4 解决“哪些词值得再次遇见”。V5 解决“下一篇最适合读哪一篇”。

V5 是一个多目标文章排序层，组合以下信号：

`词汇机会 + 阅读兴趣 + 难度匹配 + 学习目标 + 新鲜度 + 篇幅`

FSRS 仍负责底层遗忘预测；V5 不替换 FSRS，也不把用户变成传统的“开始复习 / 结束复习”流程。

## 已实现边界

- `src/lib/recommendationV5.ts` 提供纯函数评分和排序，不访问存储、不调用网络。
- `V5RecommendationProfile` 是显式输入；没有传入 `v5Profile` 时，V2.2/V4 推荐结果保持原行为。
- `MemoryV2RecommendationAdapter` 支持通过 `v5Profile` 对已有推荐结果重新排序，并保留文章 ID、复习命中数和 V4 推荐原因。
- 所有特征先归一化到 `[0, 1]`，最终分数为 `[0, 100]`，便于解释、监控和后续校准。

## 评分契约

默认权重如下：

| 特征 | 权重 | 作用 |
| --- | ---: | --- |
| Opportunity | 0.28 | 优先覆盖 V4 计算出的高机会词和到期词 |
| Interest | 0.24 | 主题命中用户当前兴趣；无偏好时使用中性分 |
| Difficulty | 0.15 | 文章 CEFR 与用户档位相同或相邻时得分更高 |
| Goal | 0.18 | IELTS、GRE、academic、fluency 等目标可提高相关主题 |
| Novelty | 0.08 | 降低近期连续出现的同主题，防止推荐单一化 |
| Length | 0.07 | 匹配目标阅读时长或词数区间 |

`v5Profile.weights` 可以覆盖这些权重，但实现会重新归一化，避免调用方把总分推到不可比较的范围。

### 特征来源

- Opportunity：来自每个词的 V4 `opportunityScore`，同一词在文章中只计一次；没有 V4 词机会时，使用到期词数量作为保守回退。
- Interest：来自用户主动选择的主题或当前推荐请求主题。
- Difficulty：来自文章的 `levelRating.level`、`level` 或 `rewriteTargetLevel`，与用户测评得到的 CEFR 比较。
- Goal：来自显式目标配置和文章主题 / 目标标签；当前目标标签接口已预留，默认仅使用主题提示。
- Novelty：来自最近阅读文章的主题；已排除的文章仍然不会再次推荐。
- Length：来自文章估计词数和用户目标区间；缺少词数时使用中性分，不淘汰文章。

## 需要收集什么，以及为什么

V5 只需要已有阅读数据的派生结果，不需要新增隐蔽采集：

| 数据 | 是否原始内容 | 用途 |
| --- | --- | --- |
| 文章打开 / 完成 / 最近阅读时间 | 否，阅读行为元数据 | 排除已读文章、计算新鲜度和连续阅读节奏 |
| 词曝光、点击、上下文、FSRS 状态 | 词汇学习证据 | 计算 Opportunity，选择最值得再次遇见的词 |
| CEFR 测评结果 | 否，测评摘要 | 控制文章难度和推荐长度 |
| 用户主动选择的主题 / 学习目标 | 否，偏好配置 | 调整 Interest 和 Goal 权重 |
| 文章主题、级别、词数 | 否，文章元数据 | 计算难度、兴趣、篇幅特征 |

推荐排序不上传整篇文章。文章正文只有在用户主动使用翻译、查词、改写或讨论等 AI 功能时，才按该功能所需范围发送到服务端；常规记忆与 V5 排序默认保存在当前浏览器。

## 推荐闭环

```text
阅读文章
  -> V4 记录词曝光并更新 FSRS / Opportunity
  -> V5 计算候选文章多目标分数
  -> 过滤已读与不合适的候选
  -> 推荐下一篇
  -> 记录新的阅读证据
```

## 上线策略

1. 先使用 `v5Profile` 在开发环境和小流量用户上对比 V4 排序，记录点击、完成率、跳过率和有效曝光覆盖。
2. 只有在 V5 的完成率不下降、且 Opportunity Coverage 提升时，才扩大启用范围。
3. 未来加入目标配置页后，再把 GRE / IELTS 等目标写入用户档案；眼动、AI 质量判断等高敏感信号不属于当前 V5 必需数据。
4. 任何新的服务端诊断字段都必须继续遵守“最小化、可说明、非整篇文章上传”的边界。

## 约束

- V5 不能绕过已读排除、CEFR 安全窗口或强制曝光词的最低机会分。
- 推荐分数只是排序工具，不直接改变 FSRS 评级或词汇掌握等级。
- 分数特征必须保持可解释；引入学习排序模型前，先保留当前规则分数作为基线和回退。
